---
// kind: info / success / warning / danger を渡すと DS の状態色ペアで表示
const { title, kind } = Astro.props;
---
<div class={kind ? `callout ${kind}` : 'callout'}>
  {title && <span class="ttl">{title}</span>}
  <slot />
</div>
